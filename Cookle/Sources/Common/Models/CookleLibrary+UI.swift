import SwiftUI

extension Tag {
    static var title: LocalizedStringKey {
        .init(titleKey)
    }
}

extension DiaryObjectType {
    var title: LocalizedStringKey {
        .init(titleKey)
    }
}
