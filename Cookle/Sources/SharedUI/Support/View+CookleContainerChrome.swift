import MHUI
import SwiftUI

extension View {
    func cookleListChrome() -> some View {
        mhListChrome()
    }

    func cookleFormChrome() -> some View {
        mhFormChrome()
    }

    func cookleScreenChrome() -> some View {
        mhScreen()
    }
}
